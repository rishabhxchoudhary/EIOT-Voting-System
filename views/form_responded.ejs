<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= result.course %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"></link>
	<script src="https://kit.fontawesome.com/b2255bbbaf.js" crossorigin="anonymous"></script>
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> -->
</head>

<style>
body, html{
    min-width: 460px;
}
/* Background */
.video-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  background: #131623;
  z-index: -2;
}
.video-bg::before{
    --i : 4;
    content: '';
    position: absolute;
    width: 990px;
    height: 990px;
    border-radius: 50%;
    background: linear-gradient(rgb(0,47,255),#f0f);
    transform: translate(-90%,-40%);
    z-index: -1;
    animation: animate2 10s linear infinite;
    animation-delay: calc(-1s * var(--i));
}

.video-bg::after{
    position: relative;
    --i : 1;
    content: '';
    /* position: absolute; */
    width: 990px;
    height: 990px;
    border-radius: 50%;
    background: linear-gradient(#f0f,rgb(0,47,255));
    transform: translate(100%,40%);
    z-index: -1;
    animation: animate1 10s linear infinite;
    animation-delay: calc(-1s * var(--i));
}

@keyframes animate1 {
    0%,100%{
        transform: translate(110%,45%);
    }
    50%{
        transform: translate(90%,35%);
    }
}

@keyframes animate2 {
    0%,100%{
        transform: translate(-80%,-30%);
    }
    50%{
        transform: translate(-100%,-50%);
    }
}

video {
    position: absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* Nav */
*{
    padding: 0;
    margin: 0;
    text-decoration: none;
    list-style-type: none;
    box-sizing: border-box;
}

body{
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
}


nav{
    position: fixed;
    text-transform: uppercase;
    background: transparent;
    height: 80px;
    width: 100%;
    background-color: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(4.5px);
    box-shadow: 20px 20px 40px -6px rgba(0, 0, 0, 0.2);
    display: flex;
    align-items: center;
    justify-content: space-between;
}


label.logo{
    color: white;
    font-size: 35px;
    line-height: 80px;
    padding: 0 100px;
    font-weight: bold;
}

nav ul{
    float: right;
    margin-right: 20px;
}

nav ul li{
    display: inline-block;
    /* line-height: 80px; */
    margin: 0 5px;
}

nav ul li a{
    color: white;
    /* font-size: 17px; */
    text-transform: uppercase;
    padding: 7px 13px;
    border-radius: 3px;
}

nav ul li a.active, a:hover{
	display: inline-block;
    /* padding: 10px 15px; */
    border: 0;
    text-decoration: none;
    border-radius: 15px;
    background-color: rgba(255,255,255,0.1);
    border: 1px solid rgba(255,255,255,0.1);
    backdrop-filter: blur(30px);
    color: rgba(255,255,255,0.8);
    font-size: 14px;
    letter-spacing: 2px;
    cursor: pointer;
    text-transform: uppercase;
}

.checkbtn{
    font-size: 30px;
    color: white;
    float: right;
    line-height: 80px;
    margin-right: 40px;
    cursor: pointer;
    display: none;
}

#check{
    display: none;
}

@media(max-width: 952px){
    label.logo{
        font-size: 30px;
        padding-left: 50px;
    }
    nav ul li a{
        font-size: 16px;
    }
}
@media(max-width: 858px){
    .checkbtn{
        display: block;
    }
    nav ul{
        position: fixed;
        width: 100%;
        height: 100vh;
        background: #2c3e50;
        top: 80px;
        left: -100%;
        text-align: center;
        transition: all 0.5s ;
    }
    nav ul li{
        display: block;
        margin: 50px 0;
        line-height: 30px;

    }

    nav ul li a{
        font-size: 20px;
    }
    nav a:hover, a.active{
        background: none;
        color: #0082e6;
    }

    #check:checked ~ ul{
        left: 0;
    }

	.charts span::before{
		font-size: 10px;
		word-wrap: normal !important;
	}

	.charts span{
		margin: 0 10px !important;
	}
}

/* Footer CSS */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;400;500;600;700;800;900&display=swap');
*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
}
body{
    line-height: 1.5;

}

.container{
    max-width: 1170px;
    margin: auto;
}

ul{
    list-style: none;
}

.footer{
    background-color: #24262b;
    padding: 70px 0;
}

.row{
    display: flex;
    flex-wrap: wrap;
}

.footer-col{
    width: 25%;
    padding: 0 15px;
}

.footer-col h4{
    font-size: 18px;
    color: white;
    text-transform: capitalize;
    margin-bottom: 35px;
    font-weight: 500;
    position: relative;
}

.footer-col h4::before{
    content: "";
    position: absolute;
    left: 0;
    bottom: -10px;
    background-color: #e91e63;
    height: 2px;
    box-sizing: border-box;
    width: 50px;
}

.footer-col ul li:not(:last-child){
    margin-bottom: 10px;
}

.footer-col ul li a{
    font-size: 16px;
    text-transform: capitalize;
    color: #bbbbbb;
    text-decoration: none;
    font-weight: 300;
    display: block;
    transition: all 0.3s ease;
}

.footer-col ul li a:hover{
    color: #fff;
    padding-left: 8px;
}

.footer-col .social-links a{
    display: inline-block;
    height: 40px;
    width: 40px;
    background-color: rgba(255,255,255,0.2);
    margin: 0 10px 10px 0;
    text-align: center;
    line-height: 40px;
    border-radius: 50%;
    color: white;
    transition: all 0.5s ease;
}
.footer-col .social-links a:hover{
    color: #24262b;
    background-color: #fff;
}

/* Responsive */
@media(max-width: 767px){
    .footer-col{
        width: 50%;
        margin-bottom: 30px;
    }
}
@media(max-width: 574px){
    .footer-col{
        width: 100%;
        /* margin-bottom: 30px; */
    }
	.content{

	}
	.content h1{
		font-size: 18px;
	}

	.content h3{
		font-size: 12px;
	}
}

/* Content */

.content{
  /* @include display-flex; */
  position: absolute;
  width: 85%;
  height: 70%;
  background-color: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(4.5px);
  box-shadow: 20px 20px 40px -6px rgba(0, 0, 0, 0.2);
  border-radius: 25px;
  padding:20px;
  /* overflow: scroll; */
  display: flex;
  /* justify-content: space-around; */
  gap: 10px;
  align-items: center;
  flex-direction: column;
  color: white;
}


/* Chart */
.charts{
  display:table;
  table-layout: fixed;   
  
  width:100%;
  max-width:900px;
  height:50%;
  margin: 10px;
  margin:0 auto;
  
  background-image:linear-gradient(bottom, rgba(0, 0, 0, 0.1) 2%, transparent 2%);
  background-size: 100% 50px;
  background-position: left top;
	border: 1px solid white;
}
  
.charts  li{
    position:relative;
    display:table-cell;
    vertical-align:bottom;
    height:200px;
	border: 1px solid white;
}
  
.charts  span{
	color: white;
    margin:0 2em;
    display: block;
    background-color: rgba(255, 255, 255, 0.);
	backdrop-filter: blur(10px);
	-webkit-backdrop-filter: blur(4.5px);
	box-shadow: 20px 20px 40px -6px rgba(0, 0, 0, 0.2);
    animation: draw 1s ease-in-out;
	border: 1px solid white;
}
    
.charts span::before{
	display: flex;
	flex-wrap: wrap;
      position:absolute;
      left:0;right:0;top:100%;
      padding:5px 1em 0;
  		display:block;
      text-align:center;
      content:attr(title);
      word-wrap:break-word ;
	  border: 1px solid white;
    }

@keyframes draw{
  0%{height:0;} 
}


/* END CHECKBOX STYLE */
</style>
<body>
    <!-- Nav -->
    <nav>
        <input type="checkbox" id="check"/>
        <label for="check" class='checkbtn'>
            <i class='fas fa-bars'></i>
        </label>
        <label for="" class="logo"><%= title %></label>
        <ul>
            <li><a class='active' href="/">Home</a></li>
            <li><a class='active' href="/route/logout" method="post">Logout</a></li>
        </ul>
    </nav>







    <!-- Content -->
    <div class="video-bg">
        
        <div class="content">
            <!-- <table class="table table-striped">
                <tr><th>Course :</th><th><%= result.course %></th></tr>
                <tr><th>Date :</th><th><%= result.date %></th></tr>
                <tr><th>Cancel Count :</th><th><%= result.cancel.length %></th></tr>
                <tr><th>Reschedule Count :</th><th><%= result.reschedule.length %></th></tr>
                <tr><th>Dont Change Count :</th><th><%= result.noChange.length %></th></tr>
                <tr><th>Mass Bunk Count :</th><th><%= result.massBunk.length %></th></tr>
                <tr><th>Total Vote Count :</th><th><%= result.responded.length %></th></tr>
            </table>
            <span></span> -->

                <h1><%= result.course %></h1>
				<h3><%= result.date %></h3>
				<ul class="charts">
					<li>
						<span style="height: <%= (result.cancel.length/result.responded.length)*100 %>%" title="Cancel  <%= (result.cancel.length/result.responded.length)*100 %>%"></span>
					</li>
					<li>
						<span style="height: <%= (result.reschedule.length/result.responded.length)*100 %>%" title="Reschedule <%= (result.reschedule.length/result.responded.length)*100 %>%"></span>
					</li>
					<li>
						<span style="height: <%= (result.noChange.length/result.responded.length)*100 %>%" title="Don't Change <%= (result.noChange.length/result.responded.length)*100 %>%"></span>
					</li>
					<li>
						<span style="height: <%= (result.massBunk.length/result.responded.length)*100 %>%" title="Mass Bunk <%= (result.massBunk.length/result.responded.length)*100 %>%"></span>
					</li>
				</ul>    

        </div>
    </div>


</body>
</html>